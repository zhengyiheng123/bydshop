<resources>
    <!-- Base application theme. -->
    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/back_blue</item>

        <item name="android:textSize">@dimen/tv_24px</item>


    </style>
    <style name="StartStyle" parent="android:Theme.Translucent.NoTitleBar.Fullscreen">
    </style>
    <style name="AppTheme" parent="BaseAppTheme">
        <!-- Customize your theme here. -->
    </style>
    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">-->
        <!--&lt;!&ndash; Customize your theme here. &ndash;&gt;-->
    <!--</style>-->



    <!--<item name="android:listDivider">@drawable/listview_fengexian</item>-->
    <style name="CustomDialog" parent="@android:style/Theme.Dialog">
    <item name="android:windowCloseOnTouchOutside">false</item>
    <item name="android:windowFrame">@null</item>
    <!-- 去掉dialog标题 -->
    <item name="android:windowNoTitle">true</item>
    <!-- 设置是否悬浮 -->
    <item name="android:windowIsFloating">true</item>
    <item name="android:windowContentOverlay">@null</item>
    <!--   将dialog背景色置为透明   -->
    <item name="android:windowBackground">@android:color/transparent</item>
    </style>
    <!--dialog动画-->
    <style name="dialogWindowAnim" mce_bogus="1" parent="android:Animation">

        <!--<item name="android:windowEnterAnimation">@anim/dialog_enter_anim</item>-->
        <!--<item name="android:windowExitAnimation">@anim/dialog_exit_anim</item>-->
    </style>

    <!--设置dialog的背景样式，为透明-->
    <style name="DialogTheme">
        <item name="android:windowFrame">@null</item>
        <!-- 去掉dialog标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 设置是否悬浮 -->
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <!--   将dialog背景色置为透明   -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="DialogTheme1" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 去掉dialog标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 设置是否悬浮 -->
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <!--   将dialog背景色置为透明   -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="articlelist_item_ratingbar_style" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/articlelist_item_rating_drawable_selector</item>
        <item name="android:numStars">5</item>

    </style>
</resources>
